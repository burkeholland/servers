We want to add the buttons to the top of the README, but we want to also add a section that instructs people on how to enter the settings as either a docker container or a node module. We want the section to be above the license so it's not the last thing in the file. for manual installation, we want to say "Add the following to your .vscode/mcp.json file:". Make sure to use the mcp-server-to-vscode tool before you being doing updates.
